{
  "name": "yieldguard-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "next": "15.3.8"
  },
  "engines": {
    "node": "20.x"
  }
}
